import './utils.js';

/* This presently serves no purpose.

It is a placeholder to contain methods with which to perform business logic
as helpers for AppContainer.js in order to increase readability. (to be imported into AppContainer.js).

const ledger = (deposit, balance) => {
    let msg = `Your deposit is ${deposit}
    added to your previous balance (${balance})
    makes your new balance ${deposit + balance}`;
    console.log( msg);
}

export default ledger;

*/